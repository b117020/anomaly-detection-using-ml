import os
os.getcwd()
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train001"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"1_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train002"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"2_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train004"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"4_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train005"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"5_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train006"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"6_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train007"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"7_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train008"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"8_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train009"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"9_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train010"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"10_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train011"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"11_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train012"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"12_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train013"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"13_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train014"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"14_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train015"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"15_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train016"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"16_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train017"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"17_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train018"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"18_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train019"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"19_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train020"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"20_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train021"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"21_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train022"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"22_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train023"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"23_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train024"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"24_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train025"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"25_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train026"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"26_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train027"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"27_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train028"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"28_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train029"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"29_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train030"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"30_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train031"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"31_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train032"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"32_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train033"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"33_"+str(i)+".jpg")
        os.rename(absname, newname)
dirname = "C:\\Users\\DELL\\Desktop\\UCSD_Anomaly_Dataset.v1p2\\UCSDped1\\Train\\Train034"
for root, dirs, files in os.walk(dirname):
    for i,f in enumerate(files):
        absname = os.path.join(root, f)
        newname = os.path.join(root,"34_"+str(i)+".jpg")
        os.rename(absname, newname)

